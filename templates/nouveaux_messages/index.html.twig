{% extends 'base.html.twig' %}

{% block title %}
	NouveauxMessages
{% endblock %}

{% block body %}
	<a href="{{ path('app_user') }}">X</a>


	<div class="max-w-lg mx-auto p-6 bg-white rounded-lg shadow-lg mt-10">
		<h1 class="text-2xl font-semibold text-gray-800 mb-4">Créer de nouveaux messages</h1>

		{{ form_start(form) }}

		<div class="mb-4">
			{{ form_row(form.title, {
                'attr': {'class': 'w-full p-3 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500'}
            }) }}
		</div>

		<div class="mb-4">
			{{ form_row(form.Contente, {
                'attr': {'class': 'w-full p-3 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500'}
            }) }}
		</div>

		<div class="mb-4">
			{{ form_row(form.add_new_message, {
                'attr': {'class': 'w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-600'}
            }) }}
		</div>

		{{ form_end(form) }}
	</div>
{% endblock %}
